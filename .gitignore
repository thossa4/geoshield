__pycache__/
*.pyc

# Generated report/data outputs — regenerated by running the pipeline,
# not source. property_indicators.csv in particular accumulates rows
# across dev runs with no schema migration, so it isn't meaningful
# tracked history.
data/processed/*.csv
data/cache/
reports/*.html
